# Global Nginx configurations (http context)
server_tokens off;

# Global gzip settings
gzip on;
gzip_vary on;
gzip_min_length 10240;
gzip_proxied expired no-cache no-store private auth;
gzip_disable "MSIE [1-6]\.";